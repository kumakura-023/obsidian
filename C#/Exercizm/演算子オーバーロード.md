---
created: 2025-12-04T13:38
updated: 2025-12-04T13:38
---
# 演算子オーバーロードとは

以下は、金額を扱うクラスで、フィールドに金額と単位を持つ。

```C#
public struct CurrencyAmount
{
    private decimal amount;
    private string currency;

    public CurrencyAmount(decimal amount, string currency)
    {
        this.amount = amount;
        this.currency = currency;
    }
}
```

このクラスのインスタンスAとBがあるとして、それらを直接比較するとクラスとして一致するかどうかを見るだけで、金額（＝金額と単位）の一致を見ることはできない。
例 ：A = 100円,B= 100ドル  
	A == B →false
	A  != B →true